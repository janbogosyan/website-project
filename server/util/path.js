// const path = require('path')

// module.exports = path.dirname(process.mainModule.filename) //path to the parent directory,


//mainModule will refer to the main directory where we start our app , so it will be app.js in our case
//izpolzvame go v admin.js faila